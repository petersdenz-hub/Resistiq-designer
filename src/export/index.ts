/**
 * Export will later turn a Design Document into preview images,
 * production files, and tech packs.
 *
 * Nothing is exported yet. The document stays structured so those
 * pipelines can be added without flattening the design into one image.
 */
export const EXPORT_AVAILABLE = false
