import { GarmentCustomization } from '@/studio/properties/GarmentCustomization'

/** Kept for compatibility. Fabric and details now live in the right properties panel. */
export function MaterialsPanel() {
  return <GarmentCustomization />
}
